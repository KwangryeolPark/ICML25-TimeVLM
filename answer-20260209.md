# Time-VLM 모델 분석 답변

## 질문 1: 따로 vision 데이터를 받는 건가 아니면 시계열 데이터를 받으면 내부적으로 비전 데이터로 바꾸나?

**답변:** 시계열 데이터를 받으면 **내부적으로 비전 데이터(이미지)로 변환**합니다.

모델은 원시 시계열 데이터를 입력으로 받아 내부에서 이미지로 변환하는 과정을 거칩니다:

1. **입력**: 시계열 데이터 `x_enc` (shape: `[B, seq_len, n_vars]`)
2. **변환 과정**: 
   - `vision_augmented_learner()` 메서드를 통해 시계열을 이미지로 변환
   - 두 가지 변환 방식 지원:
     - **Learnable 방식** (`learnable_image=True`): `LearnableTimeSeriesToImage` 모듈을 사용하여 1D/2D convolution 레이어를 통해 학습 가능한 방식으로 이미지 생성
     - **Simple 방식** (`learnable_image=False`): `time_series_to_simple_image()` 함수를 사용하여 periodicity 기반으로 2D로 reshape 후 bilinear interpolation으로 이미지 생성
3. **출력**: RGB 이미지 텐서 (shape: `[B, 3, H, W]`, 기본 이미지 크기 56x56)

이미지 변환은 시계열의 트렌드, 주기적 패턴, 다중 스케일 특징을 시각적으로 표현하기 위해 수행됩니다.

## 질문 2: 단일 데이터셋에 대해, Text는 동일한 것을 사용하는가 아니면 instance (batch)마다 다른 text가 들어가는가?

**답변:** **Batch의 각 instance마다 다른 text가 생성**됩니다.

`text_augmented_learner()` 메서드는 각 배치 샘플마다 개별적으로 텍스트 프롬프트를 생성합니다:

1. **배치별 처리**: 각 배치 인덱스 `b`에 대해 개별 통계 계산
2. **동적 생성 내용**:
   - 각 instance의 최소값 (min_value)
   - 각 instance의 최대값 (max_value)
   - 각 instance의 중앙값 (median_value)
   - 각 instance의 전체 트렌드 방향 (upward/downward)
3. **프롬프트 구조** (instance마다 다른 값):
   ```
   "The time series is converted into an image using 1D and 2D convolutional layers, 
   highlighting trends, periodic patterns, and multi-scale features for forecasting.
   Dataset: {description}
   Task: Forecast the next {pred_len} steps using the past {seq_len} steps.
   Input statistics: min value = {min_value:.3f}, max value = {max_value:.3f}, 
   median value = {median_value:.3f}, the overall trend is {trend_direction}."
   ```

따라서 동일한 데이터셋이라도 각 배치 샘플의 실제 값에 따라 다른 텍스트가 생성됩니다.

## 질문 3: 입력 형태는 무엇이고 출력 형태는 무엇이면 representation의 형태는?

**답변:**

### 입력 형태:
- **원시 입력**: `x_enc` - shape `[B, seq_len, n_vars]`
  - `B`: batch size (기본 32)
  - `seq_len`: 입력 시퀀스 길이 (기본 512)
  - `n_vars`: 변수 개수 (데이터셋에 따라 다름, 예: ETTh1은 7개)
- **보조 입력**: `x_mark_enc`, `x_dec`, `x_mark_dec` (시간 특징, 옵션)

### Representation 형태:
1. **Patch Embeddings**: `[B * n_vars, n_patches, d_model]`
   - `n_patches`: 패치 개수 (seq_len과 patch_len에 따라 결정)
   - `d_model`: 모델 차원 (기본 128)

2. **Vision Embeddings**: `[B, hidden_size]`
   - VLM 타입에 따라 hidden_size 다름:
     - CLIP: 512
     - BLIP2: 2560
     - ViLT: 768

3. **Text Embeddings**: `[B, hidden_size]`
   - Vision embeddings와 동일한 차원

4. **Multimodal Features**: `[B, n_vars, d_model]`
   - Vision과 Text embeddings를 결합하여 생성
   - Cross-modal attention을 통해 temporal features와 융합

### 출력 형태:
- **최종 예측**: `y` - shape `[B, pred_len, n_vars]`
  - `pred_len`: 예측 길이 (96, 192, 336, 720 등)
  - 정규화 해제(denormalization)가 적용된 최종 예측값

## 질문 4: scratch training인가 아니면 pretrain된 것을 가져와서 사용하는 것인가?

**답변:** **Pretrained VLM 모델을 가져와서 사용**하되, **시계열 관련 부분은 scratch로 학습**합니다.

### Pretrained 부분:
1. **VLM 백본**:
   - CLIP: `'openai/clip-vit-base-patch32'`
   - BLIP2: `'Salesforce/blip2-opt-2.7b'`
   - ViLT: `'dandelin/vilt-b32-finetuned-coco'`
   - Hugging Face Transformers에서 pretrained weights 로드

2. **Fine-tuning 옵션**:
   - `finetune_vlm` 파라미터로 제어 (기본값: `False`)
   - `False`일 때: VLM 파라미터를 freeze하고 사용
   - `True`일 때: VLM 파라미터도 함께 학습

### Scratch 학습 부분:
1. **시계열 특화 모듈**:
   - `PatchEmbedding`: 시계열을 패치로 임베딩
   - `LearnableTimeSeriesToImage`: 학습 가능한 이미지 생성 모듈
   - `PatchMemoryBank`: 패치 메모리 뱅크

2. **예측 헤드**:
   - `memory_head`: 메모리 기반 예측
   - `temporal_head`: 시간적 특징 처리
   - `multimodal_head`: 멀티모달 특징 처리

3. **융합 메커니즘**:
   - `multimodal_enhancement`: 비전-텍스트 융합
   - `cross_attention`: Cross-modal attention
   - `memory_fusion_gate`: 메모리 융합 게이트
   - `fusion_layer`: 최종 융합 레이어

### 학습 전략:
- **Transfer Learning 접근**: Pretrained VLM의 강력한 vision-language 이해 능력을 활용
- **효율성**: VLM을 freeze함으로써 학습 파라미터 수를 줄이고 효율적인 학습 가능
- **데이터 효율성**: Few-shot 학습 (10% 데이터) 및 Zero-shot transfer learning 지원
